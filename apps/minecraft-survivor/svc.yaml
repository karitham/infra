apiVersion: v1
kind: Service
metadata:
  labels:
    service: mc-survivor
  name: mc-survivor
spec:
  ports:
    - port: 25565
      targetPort: 25565
      nodePort: 30567
  selector:
    app: mc-survivor
  type: NodePort
